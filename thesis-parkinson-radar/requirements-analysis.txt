# Lightweight stack for the classical track (EDA + feature baselines).
# No torch — this runs on CPU without the 106 GB .mat data, off the cached
# trial feature table. For the deep track (notebooks 06/08/09/10) use the full
# requirements.txt in the conda `thesis` env instead.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
statsmodels>=0.14
